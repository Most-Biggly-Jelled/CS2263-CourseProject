package CS2263.CourseProject.UI;

/** @author  Dustin Weber
 * Should be used in all UI classes that display to screen.
 * Ensures common functions are used across each class. */
public interface InterfaceUI
{
    /** Displays UI to the screen. */
    public void show();

    /** Closes/hides the UI window. */
    public void close();
}
